# Local MongoDB (Default)
MONGO_URI=mongodb://127.0.0.1:27017/devtrackai

# Recommended: MongoDB Atlas (Cloud)
# Replace <username>, <password>, and <cluster-url> with your Atlas details
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/devtrackai

PORT=5000
# JWT_SECRET=supersecretjwtkey_devtrackai
